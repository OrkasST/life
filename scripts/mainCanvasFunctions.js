// import {
//   clear,
//   drawRect,
//   drawText,
//   drawLine,
// } from "./utils/mainCanvasUtils";

// //for animated projects
// export function animate(data, time, ctx, screen) {
//   data = update(data, time);
//   render(time, ctx, screen);
//   requestAnimationFrame((time) => animate(data, time, ctx, screen));
// }

// function render(time, ctx, screen) {
//   clear(ctx, screen);
// }

// function update(data, time) {
//   return data;
// }

// //for non-animated projects
// export function drawOnScreen(ctx) {
//   // drawRect({filled: false}, ctx);
//   // drawLine({}, ctx);
//   drawText({}, ctx);
// }
